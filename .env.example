# === Database Configuration ===
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=mpc_plus
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# === Backend Configuration ===
# The connection string can be constructed from the components above or provided directly
Database__ConnectionString=Host=localhost;Port=5432;Database=mpc_plus;Username=postgres;Password=postgres
CORS_ORIGINS=http://localhost:3000

# === Frontend Configuration ===
# URL of the Backend API
NEXT_PUBLIC_API_URL=http://localhost:5132/api

# === Optional: Supabase Fallback ===
# NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
# NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=your_supabase_key
